@using SystemElementCore.Models;
<h1>
    Folder: @ViewBag.parentElement.Url
</h1>

<ul>
    @foreach (var element in ViewBag.elements)
    {
        <li>
            <a href="@element.Url">@element.Url</a>
        </li>
    }
</ul>